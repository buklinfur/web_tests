{
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "homepage": "https://buklinfur.github.io/web-tests",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "react-scripts": "^3.0.1"
  }
}
